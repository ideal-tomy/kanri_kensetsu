# 動作確認シナリオ・チェックリスト（デモ動画の代替）

実装指示書 §7 のシナリオを手動確認した結果を記録するためのテンプレです。

## シナリオ1：報告書テンプレ

- [ ] `/admin/master/report-templates` でサンプル3種が表示される
- [ ] `/admin/reports/output` でテンプレ・現場・期間を選び PDF がダウンロードできる
- [ ] `/api/reports/send` がスタブとして成功レスポンスを返す

## シナリオ2：トーク

- [ ] `/m/worker/chat` または `/m/supervisor/chat` で現場一覧が出る
- [ ] 現場を開きメッセージ送信・リアクション・ピンが動く
- [ ] `/api/chat/push/subscribe` がスタブで購読を受け付ける

## シナリオ3：工程管理

- [ ] `/admin/sites/site-1/phases` で工程とタスクが見える
- [ ] 進捗写真投稿で `photo_count` タスクの実績が増える（デモはインメモリ）

## シナリオ4：顧客管理（B2B2C）

- [ ] `/admin/master/clients` で取引先一覧が見える
- [ ] `/admin/master/end-customers` で施主サンプルが見える

記入例：

| 日付 | 実施者 | 結果 |
|------|--------|------|
| 2026-05-08 | 〇〇 | すべてチェック済み |
