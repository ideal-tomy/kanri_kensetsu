# Changelog

すべての変更点はこのファイルに記録します。バージョン番号は `MAJOR.MINOR` で、`docs/完全案.md` のリリース単位に対応します。

## v1.1 - 2026-05-08

### 追加機能（[docs/完全案.md](docs/完全案.md) §2）

- 出来高による進捗管理（§2.1）
  - タスクに `unit / plannedQty / actualQty / todayTargetQty` を追加し、`progressPct` を出来高から自動算出。
  - 出来高入力UI（`[+1] [+5] [+10]` のクイック加算、長押しで減算、「数値で入れる」モーダル）。
  - 進捗バーを4色グラデーション（青→橙→黄緑→緑）で表示し、達成度を視覚化。
  - 「中断」操作時に理由選択モーダル（雨／材料まち／人手たりない／そのほか）を挟み、`task_updates` にログ。
  - 完了時に祝福アニメーション。
- 週カレンダー型 人員配置（§2.2）
  - 縦軸＝現場 × 横軸＝月〜日 のマトリクス。
  - 日勤・夜勤を絵文字（☀／🌙）と色で区別。半日シフトは色を弱めに表示。
  - 当日変更（`status === "changed"`）は赤枠で強調。
  - スマホでは個人ビュー（自分の今週リスト）に切替可能。
  - `shift_type` ENUM を `day_full / day_am / day_pm / night_full / night_early / night_late` に拡張。
- 現場全体の進捗（§2.3）
  - `Site.overallProgress` を追加し、5刻みクイック選択＋数値入力で監督が手動で更新。
  - `siteProgressLogs` に変更履歴を残し、月次レポートでの参照に備える。

### UI/UX 改善（§3）

- ホーム画面（§3.1）
  - 各現場カードに「全体進捗バー」「今日いる人」「やってる/とまってるの件数」「未読バッジ」を追加。
  - 「じょうたい:」表記を撤廃し、独立した状態バッジに変更。
  - ボタンを「日報を見る」「今日のしごと」に統一。
- 日報フォーム（§3.2）
  - 必須マーカーを「（必須）」一括注記＋赤い丸印に整理。
  - 現場が選ばれたら画面上部に「現場：◯◯」のオレンジバッジを常時表示。
- 進捗画面（§3.3）
  - 出来高入力の統合、ステータスバッジ独立化、4色進捗バー、完了アニメ、中断理由モーダル。
- 人員ボード（§3.4）
  - 縦リスト型から週カレンダー型へ全面リライト。
  - 内部値「区分: full」等の露出を撤廃し、`shift` ラベルを `src/lib/copy.ts` に集約。
- 探す画面（§3.5）
  - 「よく探されてる」サジェストチップ（雨天 / 中断 / 不具合 など）。
  - タグから絞り込み（着工前 / 施工中 / 完了 / 不具合 / 安全確認 / 検査）。
  - 検索が空のときは「上のキーワードかタグからたどれます」と促す。

### データ・API

- store の初期データを「山田建設」シードに合わせて拡張（現場3件・タスク5件・今週の人員配置16件）。
- `PATCH /api/tasks/[id]` を `qtyDelta`／`pausedReason`／`status` 単独受付に拡張。
- `PATCH /api/sites/[id]/progress` を新設し、現場全体進捗の更新・履歴記録に対応。
- `/api/search` でタスクの `pausedReason` をキーワードに含めて検索可能に。

### Supabase マイグレーション同梱

`supabase/migrations/` にv1.1用SQLを同梱しました（実DB接続後に `supabase db push` で適用予定）。

- `0001_v1_1_quantity.sql` … `tasks` / `task_updates` の出来高カラム追加
- `0002_v1_1_shift_type.sql` … `shift_type` ENUM の再定義と既存データ（`full→day_full` 等）のマッピング
- `0003_v1_1_overall_progress.sql` … `sites.overall_progress` と `site_progress_logs` の追加
- `supabase/seed.sql` … 山田建設シード一式（会社・ユーザー・現場・タスク・今週の人員配置）
